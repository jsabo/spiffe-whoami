# build output
/spiffe-whoami
/dist/

# local renders and anything with real tenant values
/deploy/rendered/
*.local.env
*.local.yaml

# terraform working files and state (state holds the demo secret value)
aws/.terraform/
aws/*.tfstate
aws/*.tfstate.backup
aws/*.tfvars
