# spiffe://<your-teleport-cluster>/svc/payments/ledger — same project as processor,
# so the two accept each other over mTLS. No AWS role: the page shows "not configured".
NAMESPACE=payments
NAME=ledger
PEERS=processor.payments:8443,processor.analytics:8443
